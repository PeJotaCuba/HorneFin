// Servicio de IA eliminado.
// La aplicación ahora utiliza procesamiento local para archivos de texto.

export const parseRecipeFromText = async (text: string): Promise<any> => {
  throw new Error("Servicio de IA eliminado.");
};

export const parseRecipeFromImage = async (base64Image: string): Promise<any> => {
  throw new Error("Servicio de IA eliminado.");
};